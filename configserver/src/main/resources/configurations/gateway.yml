server:
  port: 8222
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: ecommerce
          uri: http://localhost:8090
          predicates:
            - Path=/api/v1/ecommerce/**
eureka:
  client:
    register-with-eureka: false